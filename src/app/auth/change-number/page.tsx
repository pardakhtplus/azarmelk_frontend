import ChangeNumberContainer from "./_components/ChangeNumberContainer";
import { type Metadata } from "next";

export const metadata: Metadata = {
  title: "تغییر شماره موبایل",
  description: "تغییر شماره موبایل",
};

export default function ChangeNumberPage() {
  return <ChangeNumberContainer />;
}
